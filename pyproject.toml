[project]
name = "token-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.2",
    "requests>=2.32.5",
    "web3>=7.14.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
